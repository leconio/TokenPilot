# 概念与计算原理

[English](concepts.md)

## 用量事件

Connector 会为每次模型尝试发送一个事件。事件包含稳定标识、模型身份、Token 和请求用量、耗时、结果、分流原因以及可信的产品上下文，不需要提示词或模型回复。Registry 对同一个事件标识只接收一次；重试会返回原结果，不会重复计算。

常见用量类型包括：

- `request`：一次模型尝试；
- `uncached_input_tokens`：没有命中缓存的输入 Token；
- `cache_read_tokens`：从服务商缓存读取的输入 Token；
- `cache_write_tokens`：写入服务商缓存的输入 Token；
- `reasoning_tokens`：支持该能力的服务商上报的推理 Token；
- `output_tokens`：模型可见输出 Token。

## 应用、调用连接、真实模型与虚拟模型

**应用**是数据隔离边界。每个应用都有独立的用户、字段、报表、模型配置和密钥，不会与其他应用混用。

**调用连接**说明应用怎样访问模型服务，可以是 LiteLLM、OpenAI 兼容服务或 Anthropic。TokenPilot 只保存 `OPENAI_API_KEY` 这样的本地凭据引用，真正的值留在应用环境中。

**真实模型**由调用连接和实际发送给服务端的模型名称组成。服务商成本备用规则和 AI Unit 换算率都绑定真实模型；同一个服务商模型如果通过两条连接调用，就建立两条真实模型记录，便于分别控制和统计。

**虚拟模型**是业务代码长期使用的稳定名称。首选模型、备用顺序、权重、时间条件、用户条件和临时切换都归它管理。新策略发布后，可以在已注册连接之间切换，而不修改应用中的虚拟模型名。

## 服务商成本

每次尝试优先使用 SDK 或 Connector 上报的实际金额。这样可以准确保留按账号协议、批处理、服务等级、促销或其他条件变化的服务商价格，不需要把它们强行简化为固定 Token 单价。事件会同时记录金额、币种以及它是否为估算值。

调用端不能提供金额时，可以按顺序配置备用成本规则。规则可使用调用内置字段，也可使用已经定义类型的事件属性和用户属性。第一条匹配规则生效，金额由可选的调用固定金额与规则中已配置的各类实际用量组成：

```text
规则成本 = 调用固定金额 + Σ（实际用量 × 每单位金额）
```

既没有上报金额、也没有匹配规则时，这次尝试会标记为未定价。如果调用端始终上报成本，可以不配置任何备用规则。上报成本保留自身币种；备用规则使用应用币种。正式结果可以通过差额记录替换临时结果，历史记录保持不可变。

## AI Unit

AI Unit 使用独立单价表。一个简单示例可以是：

```text
1,000,000 输入 Token        = 1.000000 AIU
1,000,000 缓存读取 Token    = 0.250000 AIU
1,000,000 缓存写入 Token    = 0.500000 AIU
1,000,000 推理 Token        = 4.000000 AIU
1,000,000 输出 Token        = 3.000000 AIU
```

内部使用整数微 AIU 保存结果，只在规定边界进行一次舍入，不使用浮点数。每次计算都会保存实际使用的已发布单价快照、舍入方式和尝试计算规则。

服务商成本和 AI Unit 相互独立。修改成本上报或备用规则，不会改变 AI Unit 换算、额度、预留和拉闸。覆盖页面会分别展示两类缺口。

## 虚拟模型与分流

虚拟模型是业务代码使用的稳定模型名称。它内部维护完整的分流设置，包括：

- 一个默认候选；
- 按顺序排列的失败候选；
- 零个或多个时间条件；
- 带明确过期时间的临时规则；
- 单调递增的策略修订号。

SDK 使用 ETag 拉取策略，并确认自己实际应用的精确修订。短暂故障时可以读取原子保存的最后可用策略。格式错误或已经过期的策略不会被静默接受。

## 额度生命周期

“只统计”模式只记录 AI Unit，不阻止请求。强制限制模式使用下面的过程：

```text
检查 → 预留 → 调用模型 → 结算
                    └──→ 释放
过期预留 ─────────────→ 过期
```

每个状态转换都可以安全重试。权威余额和变动记录保存在 PostgreSQL。ClickHouse 只接收用于统计的投影，不参与是否允许请求的决定。

## 数据由谁负责

| 数据                                    | 权威位置                     |
| --------------------------------------- | ---------------------------- |
| 模型目录、成本规则、AI Unit、策略、密钥 | PostgreSQL                   |
| 计算结果、用户额度、预留、变动记录      | PostgreSQL                   |
| 队列租约和短期协调状态                  | Redis                        |
| 报表和仪表盘                            | ClickHouse                   |
| SDK 或 Connector 重试缓冲               | 调用端旁边的本地 SQLite 文件 |

统计不会在 ClickHouse 故障时改读 PostgreSQL。明确显示统计不可用，比返回口径不同或不完整的数字更安全。

TokenPilot 不代理模型流量。Node、Python SDK 可以直接调用已注册连接；LiteLLM Connector 则让 LiteLLM 保持在请求链路中。两种方式使用同一份分流策略和同一种用量事件。

## 全新数据库规则

项目仍处于开发阶段。PostgreSQL 和 ClickHouse 都从空卷创建当前结构。发现未知对象或混合结构时会拒绝启动；开发环境应删除对应隔离卷，再创建当前结构。项目不保留旧结构转换逻辑。
